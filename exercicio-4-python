contador = 1
soma = 0

n = int(input('Escreva um número: '))

if n >= 0:
    while contador <= n:
        if contador % 2 == 0:
            soma = soma + contador

        contador += 1

    print(f'Soma dos pares: {soma}')
else:
    print('Número inválido.')
